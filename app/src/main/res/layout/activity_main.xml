<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_walk_through"
    tools:context=".MainActivity">

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/slideViewPager"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dimen_WT"
        android:layout_above="@+id/dotslayout"
        android:layout_alignParentTop="true"
        />

    <LinearLayout
        android:id="@+id/dotslayout"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/dimen86"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_centerInParent="true"
        android:orientation="horizontal"/>

    <Button
        android:id="@+id/btnPrev"
        android:layout_width="@dimen/dimen111"
        android:layout_height="@dimen/dimen62"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/dimen17"
        android:background="@android:color/transparent"
        android:text="@string/Back"
        android:fontFamily="@font/unicorn"
        android:textColor="@color/colorWhite"
        android:textSize="@dimen/font32"
        android:visibility="invisible" />

    <Button
        android:id="@+id/btnNext"
        android:textSize="@dimen/font32"
        android:layout_width="@dimen/dimen111"
        android:layout_height="@dimen/dimen62"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/dimen17"
        android:background="@android:color/transparent"
        android:text="@string/Next"
        android:fontFamily="@font/unicorn"
        android:textColor="@color/colorWhite" />


</RelativeLayout>